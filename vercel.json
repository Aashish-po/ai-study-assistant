{
  "version": 2,
  "buildCommand": "EXPO_USE_METRO_WORKSPACE_ROOT=1 NODE_OPTIONS=--experimental-vm-modules npx expo export -p web --output-dir dist --clear --non-interactive",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
